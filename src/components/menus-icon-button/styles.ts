import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  button: {
    padding: 16,
    width: "100%",
    borderRadius: 999,
    alignItems: "center",
    justifyContent: "center",
  },
  text: {
    fontSize: 18,
    fontWeight: "bold",
  },
});
